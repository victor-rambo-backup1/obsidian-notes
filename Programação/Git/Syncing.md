
`git pull`
- Faz `git fetch` e `git merge`

`git push -u origin <branch>`
- Permite enviar branches para o repositório remoto
- `-u` faz que quando estiver na `branch` todo o `push` seja automaticamente direcionado a ela
